---
state: done
mode: afk
priority: 7
complexity: 8
blast-radius: mid
repo: shared
origin: derived
from: done-means-done
commit: 5e2ab385b347c79204ec57c0d45ab09f47ea3db7
---

# shared-classify — the per-PRD verdict for the one `done` shared PRD with unticked acceptance boxes

The master board's scan on 2026-08-23 found one `shared` PRD carrying
`state: done` with unticked `- [ ]` acceptance boxes: `p0-foundation`,
four boxes. The master PRD (`prds/done-means-done/prd.md`) classifies
each box as closable-now with evidence; this PRD records that
classification and dispatches the per-PRD action the shared orchestrator
takes. The master PRD never ticks, strikes, or sets `blocked` on a
member's PRD — that is this PRD's job.

The classification is generated by reading `p0-foundation`'s current
`prd.md` on 2026-08-24 against the repo state on the same day; the table
below is the resulting per-row verdict, evidence, and action. The gate
that fails a regression to "tick anyway" is
`shared/conserved/tests/done_boxes_are_ticked.rs` (spec04).

## Classification

| PRD | box | verdict | evidence | action |
|---|---|---|---|---|
| `p0-foundation` | `git init` — `git init` + an initial commit of the board, the learnings, and the reset workspace | closable | `git log --reverse --oneline | head -1` → `ab154f7 foundation: the reset workspace, the board, the learnings` (the repo's first commit, 2026-08-21). The shared tree is under version control with the board and learnings in the same initial commit. | Orchestrator ticks the box |
| `p0-foundation` | `Condemn the scaffold` — delete `conserved-alloc/`, `conserved-net/`, `conserved-deriv/`, `conserved-derive/`, `conserved-core/`, and the current `conserved/src/lib.rs` | closable | Negative evidence: `find . -maxdepth 2 -name 'conserved-alloc' -o -name 'conserved-net' -o -name 'conserved-deriv' -o -name 'conserved-derive' -o -name 'conserved-core'` returns nothing on 2026-08-24 (run at `/Users/feb/dev/infra/shared`). The five condemned crates do not exist on disk. | Orchestrator ticks the box |
| `p0-foundation` | `One crate, one manifest` — `conserved/` with a valid `Cargo.toml`, tests at `conserved/tests/`, root manifest `[workspace]` only, member `conserved` | closable | `head -5 /Users/feb/dev/infra/shared/Cargo.toml` → `[workspace] / resolver = "2" / members = ["conserved"]`. `ls /Users/feb/dev/infra/shared/conserved/tests/` → `clock_instant.rs, clock_serde.rs, clock_source.rs, content_id_props.rs, content_id_serde.rs, content_id.rs, load_scope.rs, load_throughput.rs, load_unwind_panic.rs, scope.rs, smoke.rs, stats.rs` (12 test files). The root manifest is `[workspace]`-only and `conserved` is the single member. | Orchestrator ticks the box |
| `p0-foundation` | `Distribution decision` — recorded in `.mi/docs/memos/distribution.md` with status `decided` | closable | `grep '^status:\|^decided:' /Users/feb/dev/infra/shared/.mi/docs/memos/distribution.md` → `status: decided` / `decided: 2026-08-21`. The distribution memo carries the decision (`Option A, git dependency pinned by commit rev`); the `## Answers` section on the PRD already names the mechanism as settled. | Orchestrator ticks the box |

## Acceptance

- [ ] Orchestrator runs `cargo test -p conserved --workspace` in `shared/` and quotes the green output
- [ ] `p0-foundation`: four boxes ticked with the evidence quoted from the repo state above
- [ ] `conserved/tests/done_boxes_are_ticked.rs` passes with `p0-foundation` removed from the exemption list (a child PRD's completion removes the entry per spec04 acceptance box 3)
